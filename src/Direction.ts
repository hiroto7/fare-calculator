enum Direction {
    inbound = -1,
    outbound = 1
}

export default Direction;
export const inbound: Direction.inbound = Direction.inbound;
export const outbound: Direction.outbound = Direction.outbound;